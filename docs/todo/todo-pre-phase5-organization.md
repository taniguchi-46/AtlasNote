# 整理センター TODO

## 実装

- [x] AppTopBarから開けるタスク概要／候補レビューの整理センターを追加する。
- [x] フローティング／右ドック、移動・リサイズ、最小化・復帰を追加する。
- [x] アプリ内メモリにレビュー状態を保持し、閉じる／再表示で候補・適用結果を維持する。
- [x] 保存空間・Notebook・各ノートのscopeごとに解析session、選択、結果を保持し、画面／scope移動で別sessionを置き換えない。
- [x] 古い解析応答はそのsessionだけを破棄し、ロックでは全sessionを無効化して遅延応答による復元を防ぐ。
- [x] 全保存空間／Notebook直下／Notebook子孫／対象ノートと直接の関連先を読み取り専用解析する。
- [x] title、Notebook分類・移動、未分類ノート検出、タグ付与、重複・空ノートのゴミ箱移動、相互リンク提案と情報候補を追加する。
- [x] 適用時に保存空間・保護状態・revision・タグ・関連対象を再検証し、同一ノートの候補を1バッチへまとめて既存Service/CASとノート操作queueを使う。
- [x] タグ付与成功時は表示中ノートが対象と一致し、応答時も同じ場合だけ表示タグを更新する。
- [x] 保存失敗／未実行候補は再試行可能にし、競合／stale候補は再解析を案内する。
- [x] 整理とAIを`SupportWorkspace`の共通枠に統合し、AIとバックリンクからの整理操作は対象ノートscopeを開く。タブ切替・最小化・復帰で処理・選択・解析対象を保持する。
- [x] 共通パネルで対象・変更前・変更案・理由を表示し、確認後に単件／表示中の候補を一括承認できる。
- [x] 候補一覧は100件ずつ追加表示し、全件の解析結果と表示外の選択を保持する。一括選択・適用は表示中の候補に限定する。
- [x] 保存空間にタグがない場合はノートごとのタグ取得を省き、候補を変えずに解析中の割当量を抑える。
- [x] 解析中の読み取り件数・進捗バーと候補生成段階を、本文を含まないWailsイベントで表示する。要求ID・scope世代・ロック世代で古い通知を無視する。
- [x] 候補一覧の種類別件数を単一走査で集計し、表示中の選択件数を`Set`で照合する。表示件数・選択・適用の既存回帰テストと型チェックを確認する。
- [ ] 大量データの解析コストとWails上の応答性を評価し、必要な場合に限り解析上限・段階処理を調整する。合成データのGo解析時間は下記の計測記録を参照。

## 自動検証

- [x] 整理Storeの閉じる・最小化・復帰、逐次適用、失敗保持を確認する。
- [x] 空間scopeを保持したままノートA/B・sourceを移動し、選択／結果が戻ること、別scopeの逆順応答とロック後応答を確認する。
- [x] 解析失敗で直前の候補を保持することを確認する。
- [x] Go service testsで候補生成、Markdown保存、tag CAS、stale状態を確認する。
- [x] `go test ./... -count=1` を隔離APPDATA/GOCACHEで実行し、全Go package成功。
- [x] `test:organization`、`test:note-delete`、`test:auto-save`、`test:note-operation-queue`、`test:tags`、`test:note-links`、`test:notebook-hierarchy`、Frontend typecheck／lint／production build成功。
- [x] Wails v2.10.1 Windows/amd64 build成功。
- [x] `git diff --check` 成功。
- [x] 100件ずつの追加表示、表示外の選択保持、表示中の候補だけを一括適用する回帰テストを追加し、`test:organization`、Frontend typecheck／lint／production build、`go test ./internal/organize -count=1`を確認した。
- [x] 進捗のscope件数、無関係な要求・ロック後通知の無視、完了時消去をGo／Frontend回帰テストで確認した。`go test ./internal/organize ./internal/app -count=1`、`test:organization`、Frontend typecheck、bindings生成付きWails Windows/amd64 build成功。

## 合成データ計測（2026-09-24）

`BenchmarkAnalyzeOrganization`で一時SQLiteとMarkdownを作り、各ノートの本文を2,048バイトとして保存空間全体を一回解析した。AMD Ryzen 7 5700X／Windows amd64での参考値。セットアップ、Wails転送、Vue描画は計測区間外。実ノートや秘密情報は使用していない。

| ノート数 | Go解析時間 | 候補数 | 割当量 |
| ---: | ---: | ---: | ---: |
| 200 | 0.93秒 | 1,250 | 15.4MB |
| 1,000 | 3.99秒 | 6,250 | 78.9MB |
| 5,000 | 21.52秒 | 31,250 | 403.7MB |

タグ0件の場合の個別タグ取得を省いた後、同じ5,000ノート条件で再計測した結果は20.52秒、候補31,250件、割当量341.0MBだった。前後とも一回ずつの値であり、時間差は環境の揺れを含む。ノートの長さ・タグ・リンク・保護状態の分布を変えた計測と、実Wails画面の応答性は未確認。候補全件生成の費用は残るため、この数値だけで解析速度の改善完了とはしない。

## 手動受け入れ

AI内／バックリンク内の旧小型整理パネルは共通サポートパネルの対象ノートscopeを開く導線へ置き換えた。バックリンク一覧の浮動表示は維持する。

- [x] 整理／AIタブ、共通枠、最小化／再開、ノート未選択時の整理表示を実装する。
- [x] scope別整理sessionとAI入力・処理状態をタブ切替で保持し、ロック時は安全上の消去を行う。
- [x] `test:organization`、`test:ai-workspace`、`test:ai-chat`、`test:content-locks`、`test:shortcuts`、Frontend typecheck／buildを実行する。
- [ ] Wails実画面で300／320px幅、右／下／浮動、移動・サイズ変更、AI応答中と整理適用中の切替、ロック後の非表示タブを確認する。

- [ ] Wails実画面でフローティング／ドック切替、移動・サイズ変更、最小化・復帰、Notebook scope、候補の変更前／変更案／理由と承認、失敗時の再解析、AI／バックリンクから共通パネルへの対象共有、100件ずつの追加表示を確認する。Computer Useはブラウザー面のみ有効で、Windowsアプリ操作ができず未確認。
- [ ] 大規模保存空間で解析中の応答性と進捗の見せ方を評価する。

実画面の確認は実ノートを使わず、専用のテスト保存空間で行う。確認対象と期待結果:

| 操作 | 期待結果 |
| --- | --- |
| 幅300／320px、右／下／浮動へ切替、移動・サイズ変更、×最小化とアイコン再開 | エディター操作バーと重ならず、整理／AIのタブと入力・候補状態を保持する |
| 保存空間→Notebook直下→子孫→対象ノート→前の整理範囲へ戻る | scopeごとのsession・選択・結果を戻し、明示的な解析操作なしに再解析しない |
| 種類フィルターと100件超の候補を表示、選択・一括適用 | 表示外の選択を件数で示し、現在表示している候補だけを適用する |
| AI／バックリンクから整理を開く、AI応答中または整理適用中にタブ切替 | 共通枠の対象ノートscopeを開き、進行中の状態を不意に破棄しない |
| ロック、保存競合、保存失敗、再解析 | ロック時は非表示タブも消去し、競合は無変更、失敗した候補は再確認・再試行できる |
| 大規模なテスト保存空間で全件解析 | 読み取り件数・候補生成段階、操作応答、完了時間、候補表示開始までの時間、追加表示の操作感を記録する |

## 完了条件

- 変更候補を利用者が個別に確認・承認できる。
- 解析はMarkdown／SQLiteへ書き込まず、適用は既存保存・CAS・回復経路を維持する。
- 保護・ロック・ゴミ箱・stale revisionに対し安全側で停止する。
- 自動テスト成功後、手動UI受け入れと大規模データ評価の残件をstatusへ明記する。
