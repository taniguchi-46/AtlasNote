# TODO

## todoの内容

MVP (v0.1) 機能の実装。ローカルで動作するMarkdown/WYSIWYG対応の3ペイン型ノートアプリの構築を行う。

### 現状/前提

- Wails + Vue 3 + Viteプロジェクトは構築済み（`wails dev` / `wails build` 確認済み）。
- Go / Node.js 等の各種ツール類はインストール・PATH設定済み。
- SQLiteおよびMarkdown Storageを利用する責務境界・アーキテクチャの方向性は合意済みだが、コードは未実装。

### 実行手順

1. **データ層の実装 (Go)**
   - SQLite接続・初期化とテーブル作成 (ノート、ノートブック用メタデータ)
   - Markdownファイルのファイルシステム読み書き処理
   - メタデータと本文を統合して扱う Repository層 (CRUD) の実装
2. **Wails API の実装 (Go)**
   - フロントエンドから呼び出すアプリケーションサービス層の構築
   - ノート一覧取得、ノート作成、更新、削除のAPI公開
3. **フロントエンド UI基盤の構築 (Vue/TypeScript)**
   - UnoCSS, Reka UI を利用した3ペインレイアウトの作成
   - テーマ切り替え機構 (ライト/ダーク)
4. **エディタ機能の統合 (Vue)**
   - Tiptap / CodeMirror 等を用いたMarkdown/WYSIWYGエディタの導入
   - 自動保存機能の実装
5. **各機能の組み込みと結合**
   - ゴミ箱、お気に入り、ピン留め機能のUI・ロジック統合
   - ノートブック（階層構造）の表示・管理機能
   - 画像貼り付け・ドラッグ＆ドロップ対応

### 注意事項

- 実装時は関連するファイル（特に `docs/status.md` や `docs/rules/architecture.md`）を参照し、設計に矛盾が生じないようにする。
- データベースとファイルシステムのデータ整合性（エラー時のロールバック・クリーンアップ）に留意する。
- エディタコンポーネントの導入は最小構成から始め、段階的に拡張する。

### 直近の残タスク

- 完了: Tiptap の `Duplicate extension names found: ['link']` 警告を解消する。
  - `StarterKit` 側の Link 拡張を無効化し、個別に追加している `Link` 拡張だけを使う。
- 完了: 実画面確認用データの扱いを決める。
  - `C:\Users\mt252\AppData\Roaming\AtlasNote` に作成された確認用ノート・ノートブックは、MVP動作確認用として残す。
- 完了: 画像保存方式を決める。
  - MVPでは Data URL を本文に保存する方式を継続する。
  - 本文サイズ上限に当たりにくくするため、貼り付け・ドロップ可能な画像は 512KB 以下に制限する。
  - 添付ファイル保存方式への移行は、MVP後の改善タスクに回す。

### 絶対遵守事項

- 既存の Wails / Go / TypeScript / Vue 3 / UnoCSS の設計に合わせる。
- 変更範囲は依頼内容に絞る。
- 不明点は推測で固定せず、必要なら保留事項に残すか確認する。
- 実装後は可能な範囲で確認コマンド（`go test`, `npm run build`, `wails build` など）を実行する。
