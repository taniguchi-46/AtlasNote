# Phase 4 TODO

## TODOの目的

Phase 4「AI」の実装前に、AI認証・秘密情報、生成結果の保存、Phase 3 WebDAV同期との境界、プロバイダー共通契約、受け入れ条件を確定する。未確定事項を実装で先に固定しない。

詳細スコープは [`scope-phese4.md`](../development/scopes/scope-phese4.md)、現在状況は [`../status.md`](../status.md)、既存のローカル保存・競合契約は [`../development/note-concurrency.md`](../development/note-concurrency.md)、Phase 3同期契約は [`../development/webdav-sync.md`](../development/webdav-sync.md) を正とする。

## 現状・前提

- Phase 3の受け入れは、2026-07-19に非本番の実WebDAVで動作OKを確認して完了している。実サーバーまたは同期実装の更新時は回帰確認を継続する。
- Phase 4のAI実装は未着手である。
- Phase 3のWebDAV同期ではAI生成結果を同期対象外としている。Phase 4で変更する場合は、別途設計・migration・競合契約を承認する。
- API Key、アクセストークン、ローカルプロバイダー接続情報の保存方式と暗号化方針は未確定である。
- 生成結果をMarkdown本文、SQLiteメタデータ、別成果物のどこへ保存するか、また保存しないかは未確定である。

## Phase 4開始条件

Phase 4の実装開始前に、以下の必須項目を完了・レビュー承認する。

- [ ] `scope-phese4.md` の対象範囲、対象外、用語、優先順位を承認する。
- [ ] AI認証・秘密情報の保存先、セッション限定fallback、削除・更新・再認証のライフサイクルを決める。
- [ ] 生成結果の保存先、正本、版管理、ユーザー確認、削除・再生成の契約を決める。
- [ ] Phase 3 WebDAV同期との境界（同期対象外を維持するか、新しい同期契約を作るか）を決める。
- [ ] プロバイダー共通API、モデル選択、ストリーミング、キャンセル、timeout、retry、rate limit、費用上限を決める。
- [ ] 未設定・認証失敗・利用不能・オフライン・部分応答・失敗・再試行のUI状態とエラー契約を決める。
- [ ] 実キーを使わない自動テスト、秘密情報非露出、ノート本文の送信範囲を含む受け入れ条件を決める。

## 1. AI認証・セキュリティ

- [ ] API Key、アクセストークン、ローカルプロバイダー接続情報の種類と入力検証を定義する。
- [ ] OS CredentialStore、セッション限定保持、未設定状態の優先順位を決定する。
- [ ] 平文SQLite、Markdown、`localStorage`、設定ファイル、クラッシュダンプへの秘密情報保存を防ぐテストを追加する。
- [ ] ログ、エラー、通知、診断情報でAPI Key、Authorization、本文、プロンプトを非露出にする。
- [ ] endpoint、HTTPS、ローカル接続、proxy、redirect、timeout、retry、レート制限、費用上限を決定する。
- [ ] 資格情報の更新・削除・プロバイダー切り替え・再認証・secure store unavailable時の挙動を決定する。

## 2. 保存・データモデル

- [ ] 要約、タイトル、タグ、分類、関連候補、Q&A、執筆結果ごとの保存要否を決定する。
- [ ] 保存する場合の正本、スキーマ、生成元モデル、生成日時、入力版、削除・再生成方法を決定する。
- [ ] 自動適用ではなく提案・下書きとして確認する範囲を決定する。
- [ ] revision/CAS、操作journal、ノート単位lane、MarkdownとSQLiteの整合性を維持する設計を承認する。
- [ ] DB変更が必要な場合は、migration、既存データへの影響、rollback方針を実装前に記録する。

## 3. WebDAV・同期境界

- [ ] Phase 3契約のAI生成結果除外を維持するか、新しい同期対象として追加するかを決定する。
- [ ] 同期する場合のentity、manifest/object、change set、outbox、conflict、CAS、再生成重複の契約を設計する。
- [ ] 同期しない場合の端末ごとの再生成、表示、削除、バックアップ方針を決定する。
- [ ] Phase 3の `webdav-sync.md` を未承認のまま変更しないことを確認する。

## 4. プロバイダー共通契約

- [ ] Go側のprovider interface、型付きrequest/result/error、認証方式、endpointを決定する。
- [ ] Gemini、Groq、OpenRouter、OpenAI、Ollama、LM Studioの対応範囲とモデル名の扱いを決定する。
- [ ] ストリーミング、キャンセル、部分応答、入力・出力上限、context長、timeout、retryを決定する。
- [ ] 接続確認、認証失敗、rate limit、quota超過、利用不能、プロバイダー切り替え時のエラー契約を決定する。

## 5. UI・受け入れ・テスト

- [ ] 設定画面の未設定・保存・更新・削除・接続確認・再認証状態を定義する。
- [ ] AI司書、AIアシスタント、AIライティングの生成中・キャンセル・失敗・再試行・空結果・部分結果を定義する。
- [ ] ノート本文の送信前確認、機密ノート、大量ノート、長文、オフライン時の挙動を定義する。
- [ ] 実キーを使わないprovider test double、契約テスト、UI状態テスト、データ保全テストを追加する。
- [ ] AI利用不能時もローカル保存・編集・検索・既存同期が継続できることを受け入れる。
- [ ] 受け入れ記録に、使用環境・テスト対象・結果を残し、秘密情報や実エンドポイントを記録しない。

## 6. 実装着手後の順序（設計承認後）

- [ ] 認証・秘密情報境界とprovider共通契約を実装する。
- [ ] 保存・生成結果のデータモデルと既存保存laneへの接続を実装する。
- [ ] UI状態、ストリーミング、キャンセル、エラー通知を実装する。
- [ ] 保存・同期境界の受け入れテストと既存回帰テストを実行する。

## 完了条件

- 必須の設計・セキュリティ・保存・同期境界がレビュー承認済みである。
- 実キーや秘密情報をテスト・ログ・成果物へ持ち込まずに、provider契約とUI状態を検証できる。
- 生成結果の保存・破棄・再生成・競合の扱いが明文化され、既存のrevision/CAS・操作journal・同期契約と矛盾しない。
- Phase 4の実装・テスト・受け入れ結果を本TODOと `docs/status.md` に記録する。

## 絶対遵守事項

- API Key、アクセストークン、Authorizationヘッダー、ノート本文、プロンプトをログやエラーへ出さない。
- 実キーをリポジトリ、テストfixture、`.env`、SQLite、Markdownへ保存しない。
- Phase 3のWebDAV契約、DB schema、migrationを設計承認なしに変更しない。
- AI利用失敗でローカルのMarkdown正本、SQLiteメタデータ、保存lane、既存同期を壊さない。
