# Atlas Note 開発ロードマップ
---
# 実装に移る場合の注意点

## Phaseごとのブランチを分けて開発する。
基本開発ブランチをdevとし、Phaseごとにブランチを分け実装が完了した時、devにマージする。
dev → Phaseブランチ → 1機能実装ブランチ

---

# MVP（v0.1）

## 目的

毎日使えるローカルメモアプリを完成させる。

## 基本機能

- メモ作成
- メモ編集
- メモ削除
- 自動保存
- メモ同期UI（実同期はPhase 3）
- メモ検索UI（検索本体はPhase 2）
- ノート一覧
- ノートブック（階層構造）
- ノートブックアイコン
  - デフォルトアイコンを複数用意
  - ユーザー追加アイコンに対応
- ゴミ箱
- お気に入り
- ピン留め
- ウィンドウを常に最前面に配置

## エディタ

- Markdown
- WYSIWYG
- リッチテキスト
- コードブロック
- 表
- チェックリスト
- 引用
- リンク
- 表操作改善
  - `3 x 3` の表挿入
  - 行追加
  - 列追加
  - テーブル削除
  - 列削除
  - 行削除
  - `Ctrl + Enter` による新規行追加
- 表の中に表を作れてしまう不具合の修正

## UI

- 3ペインレイアウト
- Phase 3までの主要機能の先行UI実装
- メイン画面上部バー
  - メモ同期
  - メモ検索
  - 新しいノート
  - ウィンドウを常に最前面に配置
  - 設定
- 右側ノート部分の上部プルダウンメニュー削除
- ライトテーマ
- ダークテーマ
- レスポンシブ対応（最低限）

## 設定画面

- フォント設定
  - デフォルト: Meiryo
- テーマ
- 一般
  - 既定のノートブックアイコン
- エディター
  - タイポグラフィ
  - 新規ノート1行目のスタイル
  - 行の長さ、行間、段落間隔

## 保存

- SQLite
- Markdown保存

---

# Phase 2：整理・検索

詳細: `docs/development/scopes/scope-phese2.md`

## 目的

MVPで構築したローカル保存基盤と3ペインUIを維持しながら、ノートを探す、分類する、関連付けるための機能を追加する。

## 現状

- 検索入力UI、タイトル・本文検索、SQLite FTS5索引は実装済み。タグ条件によるノート検索は未実装。
- タグCRUD、ノートとの関連、タグ名の候補検索は実装済み。バックリンク、関連メモのデータ構造は未実装。
- ノートブック、お気に入り、ピン留め、ゴミ箱は実装済み。

## 実装前に決めること

- 全文検索の索引方式。SQLite FTS5、再構築可能な専用索引、外部索引を比較する。
- タグのデータモデル、制約、削除時の扱い、ノートとの関連付け方式は `docs/development/tag-design.md` で確定・実装済み。
- バックリンクの抽出規則と、関連メモの判定基準。
- 検索・フィルター・並び替えを組み合わせたときのAPIと画面状態。
- DB変更が必要な場合の既存データへの影響、migration、rollback方法。

未確定事項は実装で先に固定せず、対象機能の設計時に確定する。

## 設定

- MVPで先行実装した検索UIに実検索処理を接続する

## エディタ改善

- テーブルコピー

## タグ

- タグ追加
- タグ編集
- タグ削除

## 検索

- 全文検索
- タイトル検索
- タグ検索

## フィルター

- ノートブック
- タグ
- 更新日
- 作成日

## メモ管理

- 並び替え
- 最近開いたメモ
- バックリンク
- 関連メモ

## Phase 2と並行して対応する品質課題

- Markdownのhashまたはmtimeを使った外部編集・rename・deleteの検知とreconciliation方針を決める。
- store/APIエラーを共通通知へ接続し、batch操作の部分成功と未処理Promiseを整理する。
- `isSaving`を要求数または保存キューで管理し、並行保存中の表示を正確にする。
- MarkdownとRichの往復について、空段落、code fence、URL、多重markのテストを追加する。
- 本文をログへ出さず、operation ID、note ID、処理段階、エラー分類を記録する。
- 大量ノート対応が必要になった時点で、起動時全件読み込み、全件`Stat`、一覧ページングを見直す。

## 後続Phase着手前の必須事項

- インポート・同期前に、raw HTMLをregexだけに依存せず処理する変換・サニタイズ方針を決める。
- クラウド同期・履歴・AIストリーミング前に、revision、競合検出、保存キューの仕様を確定する。

---

# Phase 3：同期

## WebDAV

- MVPで先行実装した同期UIに実同期処理を接続する
- WebDAV同期
- 手動同期
- 自動同期
- 同期競合解決

## 将来的な同期

- Google Drive
- OneDrive
- Dropbox
- Git Repository

---

# Phase 4：AI

## AI設定

- API Key管理
- プロバイダー切り替え

対応

- Gemini
- Groq
- OpenRouter
- OpenAI
- Ollama
- LM Studio

---

## AI司書

- メモ要約
- タイトル生成
- タグ生成
- 自動分類
- 関連メモ提案
- 重複メモ検出

---

## AIアシスタント

- メモQ&A
- RAG検索
- アイデア壁打ち
- ブレインストーミング

---

## AIライティング

- プロンプト生成
- プロンプト改善
- README生成
- ドキュメント生成
- ブログ記事生成
- 要件定義生成

---

# Phase 5：Second Brain

## 思考管理

- 思考ログ
- 思考タイムライン
- 判断理由記録
- 意思決定ログ

## アイデア育成

- アイデア熟成
- AIによる改善提案
- 定期再表示
- 発展案生成

## 知識管理

- ナレッジグラフ
- 双方向リンク
- 知識マップ

---

# Phase 6：開発支援

## GitHub連携

- リポジトリ要約
- リポジトリ状態分析
- コミット履歴要約
- Issue要約
- Pull Request要約

## 開発日誌

- 今日の作業まとめ
- 実装履歴
- AI進捗分析
- 開発レポート生成

---

# Phase 7：外部連携

## LINE

- メモ追加
- 音声メモ
- OCR
- リマインダー

## Discord

- メモ追加
- メモ検索
- AI要約
- GitHub要約
- 壁打ち
- レビュー依頼

---

# 将来的な機能

- 画像貼り付け、ドラッグ＆ドロップ、添付ファイル保存
- 自動バックアップ、バックアップ復元
- グローバルショートカット
- プラグインシステム
- 追加テーマ
- マルチウィンドウ
- PDFエクスポート
- HTMLエクスポート
- Mermaid対応
- 数式（LaTeX）
- カレンダービュー
- AIエージェント
